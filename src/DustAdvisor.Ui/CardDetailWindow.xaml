<Window x:Class="DustAdvisor.Ui.CardDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Card" Height="640" Width="440" WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BgPrimaryBrush}"
        Foreground="{DynamicResource FgPrimaryBrush}"
        TextOptions.TextFormattingMode="Display"
        UseLayoutRounding="True">
  <Window.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Themes/DarkTheme.xaml"/>
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </Window.Resources>
  <Grid Margin="14">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <TextBlock x:Name="HeaderText" FontSize="18" FontWeight="Bold"
               Foreground="{StaticResource AccentGoldBrush}" Margin="0,0,0,10"/>
    <Image x:Name="ArtImage" Grid.Row="1" Stretch="Uniform"/>
    <TextBlock x:Name="DetailsText" Grid.Row="2" Margin="0,10,0,0" TextWrapping="Wrap"
               Foreground="{StaticResource FgPrimaryBrush}"/>
  </Grid>
</Window>
